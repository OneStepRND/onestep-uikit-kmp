# Test-org credentials for the test harness (testAppShared).
# Copy this file to secrets.properties (gitignored) at the repo root and fill in the values —
# they are the shared test-org credentials from the core SDK repo's demo apps.
# The generateTestAppSecrets Gradle task turns these into TestOrgSecrets.kt at build time.
# NEVER commit secrets.properties.

sdkTesting.apiKey=
sdkTesting.identitySecret=

zimmerDev.apiKey=
zimmerDev.identitySecret=

appClip.apiKey=
appClip.identitySecret=
